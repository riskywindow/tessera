# M0 gate: recon, skeleton, proof of interposition

**Status: GREEN.** Every criterion below passes and links to an artifact.
**GPU spend:** 9 GPU-attached launches, 145 s of local wall time, 44 s of container time on an L4, against a $5 cap. The dollar figure is Modal's to report; what we record is what we launched and for how long (`results/spend/ledger.jsonl`).
**Checkpoint:** HC-0. The questions are in section 5.
**Commit:** `4f52fbd`.

Two things about this gate are worth reading before the table.

**The GPU run earned its place.** 154 CPU tests passed against the fake driver
while the shim segfaulted against the real one, under both interposition
strategies. The cause was in the shim's ELF walk, in a linear symbol scan that
only runs for objects without a GNU hash table — a branch no CPU test could
reach, in one of the two files the sanitizers cannot instrument (R-22, written
down before it fired). It is fixed and the gate re-run; the backtrace is in
`results/m0/shim_diag.json`.

**Where the numbers came from.** G9's targets were written into this document
from the no-shim baseline *before* the shim was ever run against them, and the
shim then hit them exactly. Nothing here was reverse-engineered from a result.

## 0. Environment corrections found during recon

The mission brief's section 4 assumptions were checked first. Four were wrong. The corrected table is in `CLAUDE.md` §4 and summarized here:

1. **The local host is x86-64 Ubuntu 26.04**, not arm64 Ubuntu 24.04. arm64 coverage therefore comes from cross-compiling (gcc-15 and clang-20) and running the CPU tests under `qemu-aarch64`. A CI workflow for native `ubuntu-24.04-arm` also exists but cannot run until a git remote exists (question for HC-0).
2. **The local root filesystem is 100% full.** Root has about 4 GiB of reserved blocks. The human's other projects use the rest, and none of them are touched. Build trees go on tmpfs (`/tmp/tessera-build`), and apt archives were downloaded to tmpfs.
3. **`cuda.h` is in `cuda-cudart-dev-12-6`**, not `cuda-driver-dev-12-6`. The latter holds only `stubs/libcuda.so`, which we use as the authoritative export list for forwarding (640 symbols in 12.6.77).
4. Toolchains available locally: gcc 15.2, clang 20.1.8, clang-tidy 20, clang-format 19.1.7, CMake 3.31.6, Ninja 1.13.2. The brief's minimum versions (clang 18, gcc 13) are exercised in the Modal image, which is jammy-based.

## 1. Work packages

Owners are subagent roles. Subagents never commit. The orchestrator reviews each diff against the invariants and commits it per WP.

| WP | Owner | Inputs | Outputs | Invariants |
|---|---|---|---|---|
| WP-0.1 Prior-art sweep | `research` (+ independent citation verifiers) | Brief §0, §6, §7 | `docs/RELATED.md` | I-1, I-10 |
| WP-0.2 ADRs | orchestrator (fact-gathering by `research`) | Brief §6, WP-0.1 notes | `docs/DECISIONS.md`, `docs/decisions/ADR-00{1,2,3}-*.md` | I-5, I-7 |
| WP-0.3 Repo scaffold | orchestrator (skeleton) + `build-infra` | `CLAUDE.md` §5 | CMake + presets + toolchains, sanitizer configs, `.clang-format`, `.clang-tidy`, `.github/workflows/ci.yml`, `scripts/{lint_exit_codes,check_all,fetch_cuda_headers}.sh` | I-9 |
| WP-0.4 `libcuda_fake` | `fake-driver` | cuda.h 12.6, stub export list | `fake_driver/`, fake unit tests | I-3 (tests rely on it) |
| WP-0.5 `libtessera` v0 | `shim` (after WP-0.4) | WP-0.4 control API | `shim/`, `tests/shim/` | I-2, I-3, I-4, I-5 |
| WP-0.6 Modal runner | `gpu-infra` | WP-0.3 build | `infra/{modal_gpu.py,Dockerfile,versions.lock}`, `bench/samples/`, `eval/m0/`, `results/m0/`, `results/spend/ledger.jsonl` | I-1, I-3, I-9 |
| WP-0.7 Integration + review | `integration`, `reviewer` (adversarial) | all of the above | Full local matrix run, GPU gate run, review findings | all |

### Acceptance tests per WP

**WP-0.1.**
- `docs/RELATED.md` covers at least 8 neighbors, each with: what it is, the mechanism it uses, a primary source (paper or official doc URL), and a one-sentence delta stating what Tessera does that it does not, or vice versa.
- Neighbors to cover: REEF, Paella, Orion, TGS, Clockwork, HAMi-core, NVIDIA MPS, NVIDIA MIG, green contexts, cGPU, Run:ai fractional GPU, gVirtuS, and 2025–2026 green-context scheduling work.
- The file ends with a one-paragraph "Tessera's delta", which the human approves at HC-0.
- Every factual claim is checked by a verifier agent that did not write it. Any number quoted from a paper is attributed to that paper, and we assert no numbers of our own.

**WP-0.2.**
- Three ADRs exist. Each has context, decision, alternatives rejected, consequences, and the M1 measurement that could overturn it.

**WP-0.3.**
- `scripts/check_all.sh` exits 0 and prints a per-configuration summary. The configurations are:
  - native x86-64: gcc-debug, gcc-release, clang-debug, clang-release, gcc-asan (ASan+UBSan), clang-asan (ASan+UBSan), clang-tsan;
  - cross arm64: arm64-gcc and arm64-clang, with tests run under qemu-aarch64.
- All configurations use `-Wall -Wextra -Werror`.
- `scripts/lint_exit_codes.sh` has a self-test with fixtures it must reject (`|| true`, bare `set +e`, `exit 0` after a failed step) and fixtures it must accept. It passes on the repo.
- `clang-format --dry-run -Werror` is clean. `clang-tidy` reports zero findings on product sources.

**WP-0.4.** A GoogleTest suite for the fake itself covers:
- stream FIFO ordering and simulated-time kernel durations;
- event record/query/elapsed;
- stream and context sync;
- memory alloc/free and memcpy;
- `cuGetProcAddress`/`_v2` resolution by `(symbol, cudaVersion, flags)` to the correct versioned or `_ptsz` variant;
- per-symbol invocation counters readable through a control API.

It is green under ASan/UBSan and TSan.

**WP-0.5.** CPU tests against the fake cover:

- **T1 interception matrix.** Covers 2 strategies:
  - masquerade: the shim is `libcuda.so.1` on `LD_LIBRARY_PATH`, with the real driver from `TESSERA_REAL_LIBCUDA`;
  - preload: `LD_PRELOAD=libtessera.so` plus a `dlsym` hook.

  Each strategy is tested over 4 access paths:
  - direct link (`DT_NEEDED libcuda.so.1`);
  - `dlopen("libcuda.so.1")` then `dlsym(handle, …)`;
  - `cuGetProcAddress` (v1);
  - `cuGetProcAddress_v2`, including looking up `cuGetProcAddress` itself, as cudart does.

  For every hooked symbol, three counts must be equal: calls issued by the app, the shim's per-symbol counter, and the fake's per-symbol counter.
- **T2 per-thread default stream.**
  - `cuGetProcAddress(…, CU_GET_PROC_ADDRESS_PER_THREAD_DEFAULT_STREAM)` for each stream-taking hooked symbol returns a shim wrapper whose call reaches the fake's `_ptsz` variant.
  - `LEGACY_STREAM` reaches the legacy variant.
  - Versioned names resolve by `cudaVersion`: for example, `cuMemAlloc` at 3000 goes to v1 and at 12060 goes to `_v2`.
- **T3 fork.**
  - Another parent thread holds a shim-internal lock at `fork()`. The child still completes a hooked call within 2 s.
  - The child's counters start at 0, and the parent's are unchanged.
- **T4 forwarding.**
  - Non-hooked symbols reach the fake unchanged through all 4 access paths.
  - Return codes and out-parameters pass through bitwise.
- **T5 lazy init.** Loading the shim triggers zero calls into the fake until the app's first driver call.
- **T6 misconfiguration.** `TESSERA_REAL_LIBCUDA` pointing at the shim itself returns an error and does not recurse.
- **T7 I-4 preliminary.** v0 adds launch counting to the hot path, so:
  - steady-state launches under the shim make 0 heap allocations (allocation-hook count);
  - steady-state launches make 0 syscalls (strace window count, native x86-64 only).

**WP-0.6.** `modal run infra/modal_gpu.py::m0_gate` builds the image and runs on L4:
- the CPU test suite in-image (clang-18 and g++-13);
- G8 and G9 below;
- a platform pre-flight: `nvidia-smi` identity, driver and CUDA versions, and whether `nvidia-cuda-mps-control -d` starts. The pre-flight informs the M1 budget but is not an M0 criterion.

It writes `results/m0/*.json`, each containing the commit hash, GPU model, driver version, CUDA version, and exact command. Each run appends to `results/spend/ledger.jsonl`.

**WP-0.7.**
- The integration agent reruns everything from a clean build tree.
- A reviewer who did not write the code audits the shim diff against I-2, I-3, I-4, and I-5 and the traps in `CLAUDE.md`, trying to refute each test's claim.
- Findings are fixed or recorded in `docs/RISKS.md`.

## 2. Gate criteria (all hard)

The expected launch count for G9 is fixed here, before any shimmed run:
- **GEMM loop:** N iterations of `torch.mm(a, b, out=c)` with fixed shapes. The expected count is the number of kernel launches CUPTI records over the same window, via `torch.profiler` activity CUDA. This is a separate pass with the same inputs, because cuBLAS may issue more than one kernel per GEMM.
- **Elementwise control:** N iterations of `x.add_(1.0)`. The expected count is exactly N.
- **Pass condition:** the shim's counter delta over the window equals the expected count exactly for both loops.

**Pre-registered numbers.** The no-shim baseline has now been measured on an
L4 (`results/m0/gpu_baseline.json`), so the targets are written down here
before the shim is run against them, at N = 200:

| Loop | CUPTI device kernels | CUPTI host launch calls | Expected shim `launches` delta |
|---|---|---|---|
| GEMM | 400 | 200 runtime + 200 driver | **400** |
| control | 200 | 200 runtime + 0 driver | **200** |

The GEMM loop issues two device kernels per iteration, and the two are reached
by *different host paths* — one through the runtime API and one through the
driver API. That split is itself the point of the test: a shim that hooks only
one path lands on 200 and fails. `cupti_graph_launches` is 0 in both loops, so
the shim's `graph_launches` counter must also be 0 or the comparison is void.
G8's baseline hashes are recorded in the same artifact.

| # | Criterion | Target | Measured | Artifact | P/F |
|---|---|---|---|---|---|
| G1 | RELATED.md neighbors with deltas | ≥ 8, each verified | **23 neighbors**, each with a one-sentence delta; checked by two agents that did not write the file (one adversarial against primary sources, one cross-checking a separately researched 30-entry corpus). 42 corrections applied, 21 unsupported claims dropped, 13 I-10 fixes, 11 items left explicitly unverified | [`docs/RELATED.md`](../RELATED.md) | **PASS** |
| G2 | ADR-001..003 exist | 3 | 3, each with context, decision, alternatives, consequences, and the M1 measurement that would overturn it. ADR-001 additionally carries the loader measurements and two corrections found while implementing it | [`docs/DECISIONS.md`](../DECISIONS.md) | **PASS** |
| G3a | `-Werror` build, x86-64 | all configs build | 7 x86-64 configurations (gcc/clang × debug/release, gcc-asan, clang-asan, clang-tsan), 154 tests each. The Modal image builds the same tree with the brief's *minimum* compilers, g++-13 and clang-18 | [`results/m0/local_matrix.json`](../../results/m0/local_matrix.json) | **PASS** |
| G3b | `-Werror` build, arm64 | both build | 2 cross configurations (gcc-15, clang-20), tests run under `qemu-aarch64`, 154 each | same | **PASS** |
| G4 | CPU tests under ASan+UBSan | 0 failures (gcc and clang) | 0 failures in `gcc-asan` and `clang-asan`; `clang-tsan` also 0 | same | **PASS** |
| G5 | Hooked symbols intercepted via both strategies (T1) | every cell equal | 8 cells (2 strategies × 4 access paths), each requiring three independently produced counts to agree symbol by symbol, plus 4 negative controls. ≥ 8 hooked symbols compared per cell | same, `tests/shim/acceptance_matrix_test.cc` | **PASS** |
| G6 | Per-thread-stream flag handled (T2) | pass | `_ptsz` and legacy entry points both reached and counted separately; version selection across `cuMemAlloc`/`_v2` and `cuCtxCreate` v2/v3/v4; 0 stream failures, 0 version failures, in both strategies and the no-shim control | `tests/shim/acceptance_ptsz_test.cc` | **PASS** |
| G7 | Fork handler (T3) | pass | A child forked while the shim holds its init lock (confirmed by the run, not assumed: `stalled_in_shim_dlopen=1`, fork blocked ~300 ms) completes its hooked call in well under the 2 s bound, in both strategies, with counters starting from zero | `tests/shim/acceptance_fork_test.cc` | **PASS** |
| G8 | L4: sample outputs identical with and without the shim | SHA-256 equal | 4 of 4 hashes identical (`vector_add`, and the GEMM loop's a/b/c and the control loop's x), 0 mismatches | [`results/m0/gpu_gate.json`](../../results/m0/gpu_gate.json) | **PASS** |
| G9 | L4: shim launch count equals expected (pre-registered above) | exact | GEMM loop **400 = 400**; control loop **200 = 200**; `graph_launches` 0 in both, so the comparison is valid | same | **PASS** |

### Two honest notes on the table

- **G9's `vector_add` row is uncompared, and the criterion did not require it.** The
  pre-registered definition names the two PyTorch loops, and both were compared
  exactly. The sample is a plain C++ binary that never reads the shim's counters,
  so closing that row needs the runner to read the shim's exit-time stats file
  back; it is worth doing for H1 and is not done here.
- **T7's syscall half is x86-64 native only**, and both T7 instruments are skipped
  under the sanitizers, because there they measure the sanitizer's allocator and
  runtime rather than the shim. The claim is therefore measured in the
  unsanitized configurations, where the window contains only the shim's wrapper.

## 3. Spend

| | |
|---|---|
| Launches recorded | 11 (9 with a GPU attached) |
| GPU-attached wall time | 145 s local, 44 s inside the container |
| Failed launches | 2 (one Modal deserialization error in the probe, one the SIGSEGV that became the gate's main finding) |
| Cap | $5 |

Every launch is a line in `results/spend/ledger.jsonl` with its command, exit
code, timeout and app id. The authoritative dollar figure is Modal's, and the
human reads it at HC-0; what this project records is what it launched and for
how long (I-1). No container was left running: `modal app list` was checked
after the runs and each app stopped with its entrypoint.

## 3b. Measurements worth keeping from M0

These are M0 by-products, not gate criteria, and they are the reason parts of
M1 are now cheaper or better specified.

| Measurement | Value | Where |
|---|---|---|
| MPS starts inside a Modal container | **yes** — control daemon up, a client attached (server list non-empty), clean shutdown | `results/m0/platform.json` |
| `CUDA_MPS_ACTIVE_THREAD_PERCENTAGE=50` honoured | client saw **28 of the device's 58 SMs** | same |
| `nvidia-smi compute-policy` available | yes (Timeslice: Default) | same |
| Target GPU | NVIDIA L4, driver 580.95.05, CUDA driver version **13000**, compute capability 8.9, 58 SMs | same |
| Driver/header version gap (R-3) | the real driver exports **67 `cu*` symbols the shim does not**; the shim exports **none the driver lacks** | `results/m0/shim_diag.json` |
| Masquerade vs `LD_PRELOAD` interception | masquerade caught both the `DT_NEEDED` and the `dlopen`+`dlsym` path; `LD_PRELOAD` caught only the first | `results/m0/loader_semantics.json` |

The first three mean **M1's MPS rows need no root-capable fallback instance**,
which was the main budget risk in the M1 estimate.

## 4b. Risks discovered during M0, and what was done

| Risk | Discovered by | Mitigation chosen |
|---|---|---|
| R-21 the `cuGetProcAddress` identity check can be handed the shim's own pointer | building the shim | recognised and passed through; unrecognised variants increment `bypassed_lookups`, which H1 must read |
| R-22 two files the sanitizers cannot instrument | building the shim | **this is the one that fired**: the GPU SIGSEGV was in exactly those files. Fixed, and the linear scan is now confined to libc |
| R-27 `libcuda_fake` deadlocks a forked child | writing T3 | attributed by measurement (the same scenario over `null_driver` completes in under a millisecond), kept as a `DISABLED_` test that will prove the fix |
| R-28 `LD_PRELOAD` cannot see unhooked symbols fetched by handle | writing T1 | asserted per strategy rather than averaged away; feeds H1's choice of default |
| R-29 `LD_PRELOAD` + a shared stats path lets a wrapper process overwrite a tenant's telemetry | debugging | M2's telemetry is the per-tenant shm page; tests spawn the app directly |
| R-16 the dev host's disk is full | recon | builds on tmpfs, nothing of the human's deleted; **HC-0 question** |
| R-18 CI cannot run (no remote) | recon | workflow written and correct by construction, honestly marked unexecuted; **HC-0 question** |

## 4. Risks

Risks found in M0 are tracked in `docs/RISKS.md`. The ones that affect this gate:
- the disk-full host;
- no native arm64;
- newer cudart (from PyTorch wheels) requesting driver symbol versions the 12.6 header does not know;
- the `dlsym(RTLD_NEXT)` caller-relative semantics in the preload strategy;
- ASan runtime ordering with `LD_PRELOAD`;
- MPS availability in Modal.

## 5. What HC-0 needs from the human

The brief asks HC-0 to approve three things, and M0 surfaced three more.

**1. The delta statement** (`docs/RELATED.md`, "Tessera's delta"). Verification
narrowed it considerably, and the narrowed version is the one to approve or
reject. Three neighbours — Tally, XSched and Hummingbird — reach *finer* control
than Tessera on unmodified binaries over the stock driver, by rewriting the
tenant's device code (PTX transformation, `blockIdx` shifting, binary
instrumentation). Vitamin-E already pairs an `LD_PRELOAD` shim with a
coordination daemon over green contexts, which is Tessera's architecture. What
is left is the *combination*, held **without rewriting tenant device code**, with
per-tenant memory quotas, an explicit tail-latency objective, bitwise output
equality as a tested invariant, and fail-open behaviour. That is an availability
claim about artifacts, not a performance claim.

**2. ADR-001** (interposition). Masquerade primary, `LD_PRELOAD` secondary, H1
picks the default. The loader behaviour it rests on is measured, and the ADR
records two recipes that were wrong as written and how they were corrected.

**3. The M1 budget.** The brief caps M1 at $25. M0's probe removed the main
reason it might have needed more: MPS runs in-container, so H4/H5 need no
root-capable hourly instance. M0 used 145 s of GPU-attached time against $5.

**4. The dev host's disk is 100% full** (R-16). 147 GB of other projects, about
4 GiB of root-reserved space left. Nothing of yours has been touched — builds go
to tmpfs, the CUDA deps are 8 MB in `~/.cache`. M1 adds vLLM image layers, which
build on Modal rather than here, so this is survivable, but it is fragile and
`rsyslogd`/`journald` are spinning on the full disk and burning CPU. Do you want
space freed, and if so may I clear regenerable caches (`~/.cache/huggingface`
4.9 GB, journald 2.1 GB, apt 600 MB), or would you rather do it yourself?

**5. Should there be a git remote?** (R-18) `.github/workflows/ci.yml` covers
`ubuntu-24.04` on the minimum compilers plus native `ubuntu-24.04-arm`, and has
never executed because the repo has no remote. Pushing is outward-facing, so it
needs your say-so. Private repo under your account, or leave CI unexecuted and
rely on `scripts/check_all.sh` locally?

**6. Header/driver version gap** (R-3, now measured). The shim is built against
CUDA 12.6 headers, the L4's driver reports CUDA 13.0, and torch 2.10 ships a 12.8
runtime on top. The driver exports 67 `cu*` symbols the shim does not. Nothing in
M0 needed them, and `bypassed_lookups` makes any such gap visible rather than
silent — but H1 is where it would bite. Keep the 12.6 pin (matches the brief and
the image), or move the shim's headers to 13.x to match the driver?
